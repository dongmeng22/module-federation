{
  "name": "module-federation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "pnpm i && pnpm run install",
    "install": "npm-run-all --parallel install:*",
    "install:host": "cd host && pnpm i",
    "install:remote": "cd remote && pnpm i",
    "dev": "npm-run-all --parallel dev:*",
    "dev:host": "cd host && pnpm run dev",
    "dev:remote": "cd remote && pnpm run dev",
    "build": "npm-run-all --parallel build:*",
    "build:host": "cd host && pnpm run build",
    "build:remote": "cd remote && pnpm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
